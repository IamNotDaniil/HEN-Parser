# HEN-Parser

HEN-Parser — сервис для сбора вакансий из внешних источников и аналитики рынка: востребованные технологии, зарплатные вилки, распределение по регионам и динамика спроса по стеку.

## Текущий статус

### Уже сделано

- Подготовлен стартовый Python-парсер для `hh.ru`.
- Добавлен CLI для выгрузки вакансий по поисковому запросу.
- Реализовано сохранение сырых данных в `JSON` и `CSV`.
- Подготовлена стартовая схема PostgreSQL для хранения вакансий, компаний, локаций и снимков зарплат.
- Добавлен `docker-compose.yml` для локального запуска PostgreSQL.
- Добавлена загрузка raw JSON в PostgreSQL с записью `source_runs`, upsert вакансий и сохранением salary snapshots.

### Осталось сделать

- Связать fetch и import в единую pipeline-команду.
- Реализовать нормализацию навыков, компаний и городов.
- Собрать аналитический слой по зарплатам, навыкам и регионам.
- Поднять дашборд и отчётность.
- Автоматизировать регулярный запуск пайплайна.

## Архитектура первого этапа

На текущем этапе проект состоит из четырёх основных частей:

1. **Parser** — Python-клиент к `hh.ru`, который получает вакансии по ключевому слову.
2. **Raw storage** — файловое сохранение выгрузок в `data/raw` для отладки и повторной загрузки.
3. **Importer** — загрузка raw JSON в PostgreSQL с фиксацией source run и upsert вакансий.
4. **Database** — PostgreSQL со стартовой схемой для дальнейшего ETL и аналитики.

## Структура репозитория

```text
HEN-Parser/
├── data/raw/               # Сырые выгрузки JSON/CSV
├── db/schema.sql           # Стартовая схема PostgreSQL
├── parser/
│   ├── hh_client.py        # Клиент hh.ru API
│   ├── db.py               # Импорт raw JSON в PostgreSQL
│   ├── import_raw.py       # CLI для загрузки raw-экспорта
│   ├── main.py             # CLI входная точка парсера
│   ├── models.py           # Модели вакансий и зарплат
│   └── storage.py          # Сохранение JSON/CSV
├── scripts/bootstrap.sh    # Быстрый старт локального окружения
├── docker-compose.yml      # Локальный PostgreSQL
├── requirements.txt
└── README.md
```

## Быстрый старт

### 1. Подготовить окружение

```bash
./scripts/bootstrap.sh
```

### 2. Запустить PostgreSQL

```bash
docker compose up -d
```

После старта контейнера схема из `db/schema.sql` будет применена автоматически.

### 3. Выгрузить вакансии с hh.ru

```bash
python3 -m parser.main "Python" --pages 2 --per-page 20
```

Пример с указанием региона:

```bash
python3 -m parser.main "Go developer" --area 1 --pages 1 --per-page 10
```

По умолчанию результаты сохраняются в `data/raw/`.

### 4. Загрузить raw JSON в PostgreSQL

```bash
python3 -m parser.import_raw data/raw/<export>.json --query "Python"
```

Пример с регионом:

```bash
python3 -m parser.import_raw data/raw/<export>.json --query "Go developer" --area 1
```

Импорт создаёт запись в `source_runs`, добавляет или обновляет компании, локации и вакансии, а также сохраняет снимки зарплат.

## Что именно делает парсер

CLI-команда:

- отправляет запрос в `hh.ru API`;
- забирает несколько страниц вакансий;
- извлекает ключевые поля вакансии;
- сохраняет полный сырой payload для последующей обработки;
- создаёт два файла:
  - `JSON` — для повторной загрузки и отладки;
  - `CSV` — для быстрого просмотра и первичного анализа.

## Схема данных PostgreSQL

В `db/schema.sql` уже подготовлены базовые таблицы:

- `source_runs` — история запусков сбора и импорта;
- `companies` — компании;
- `locations` — города / локации;
- `vacancies` — основная таблица вакансий;
- `salary_snapshots` — история зарплатных значений.

Эта схема уже используется загрузчиком raw-экспортов и остаётся базой для следующего шага: построения аналитических представлений.

## Ближайший roadmap

### Шаг 1. Завершить ingestion

- импорт raw JSON в PostgreSQL добавлен;
- фиксация source run и количества загруженных вакансий добавлена;
- upsert вакансий по `source_name + source_vacancy_id` добавлен;
- осталось связать importer напрямую с fetch-командой в один pipeline.

### Шаг 2. Добавить аналитику

- извлечение навыков по словарю;
- расчёт средней/медианной зарплаты;
- агрегаты по городам и грейдам.

### Шаг 3. Поднять dashboard

- API для фильтров и агрегатов;
- графики по зарплатам и навыкам;
- экспорт CSV/HTML/PDF.


## Devlog для Telegram

Для публичного прогресса проекта добавлен файл `docs/telegram-devlog.md` с готовыми короткими постами в формате Telegram-канала:

```text
#devlog #project
HEN-Parser — стартовый каркас проекта

✅ Добавил Python-пакет для парсинга вакансий
✅ Реализовал клиент для hh.ru API
✅ Добавил CLI-команду для поиска вакансий по запросу

Следующий этап:
• Единая pipeline-команда fetch → import
• Первичный словарь навыков
• Первые SQL-агрегаты
```

## Почему проект полезен для портфолио

HEN-Parser демонстрирует реальный инженерный цикл: интеграцию с внешним API, хранение исторических данных, аналитику, визуализацию и автоматизацию. Это не просто pet-project, а заготовка под сервис, который можно показывать как полноценный data/backend продукт.
